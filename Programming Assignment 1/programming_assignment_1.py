from typing import Set
from logic import (universe, free, predicates, knowledge_base, clear_all, \
    Formula,
    Predicate, Forall, Exists, Bot, Top)


def resolution(cnf: Set[Set[Predicate]]) -> bool: 
    raise ValueError("Not yet implemented")